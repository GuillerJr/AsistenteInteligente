import AegisAudioCore

enum SwarmRoleVisuals {
    static let orderedRoles: [IPCSwarmAgentRole] = [
        .router,
        .planner,
        .criticalReasoner,
        .codeSecurity,
        .vision,
        .omni,
        .synthesizer,
    ]
}
